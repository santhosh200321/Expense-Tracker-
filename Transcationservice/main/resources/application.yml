server:
  port: 8082

spring:
  application:
    name: transaction-service
  datasource:
    url: jdbc:mysql://localhost:3306/transactiondb
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
